# DataForge Dependencies

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0
python-multipart==0.0.6
jinja2==3.1.2

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.13.1

# Vector support
pgvector==0.2.4

# forge-telemetry installed separately (see Dockerfile / render.yaml)
# -e ./forge-telemetry

# forge-compression: local-only (requires ForgeCommand); installed via pip install -e ../ForgeAgents/forge-compression
zstandard>=0.22.0

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2

# Environment
python-dotenv==1.0.0

# Redis (corpus versioning cache, async utilities)
redis>=5.0.0

# HTTP client
httpx==0.26.0

# Embeddings (choose one or more)
# Voyage AI is recommended (Anthropic-owned)
voyageai==0.2.3
openai==1.10.0
# cohere==4.47

# Utilities
# Using latest pydantic with Python 3.13 wheel support
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
httpx==0.26.0  # Already listed above, needed for TestClient
