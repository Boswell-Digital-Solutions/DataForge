# ADR 0002 — Explicit use authorization

Every eligible use must reference a current `GRANTED` authorization that lists
that exact use. Possession, ingestion, storage, retrieval, or prior use cannot
substitute for the grant. PACT/PCC and privacy ownership remain unresolved.
